# Git-sizer

[Git-sizer](https://github.com/github/git-sizer) is a tool for analyzing the size of git repositories. It looks through individual files, commits, packs, `.git` objects, history, etc and can flag things that may cause problems.
